{
  "name": "@alfred/db",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prisma": "prisma",
    "generate": "prisma generate",
    "dev:migrate": "dotenv -e ../../.env -- prisma migrate dev",
    "dev:studio": "dotenv -e ../../.env -- prisma studio",
    "dev:push": "dotenv -e ../../.env -- prisma db push",
    "deploy:migrate": "dotenv -e ../../.env -- prisma migrate deploy",
    "build": "tsup src/index.ts --format cjs --dts --out-dir dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.2",
  "dependencies": {
    "@prisma/client": "^6.19.2",
    "dotenv-cli": "^11.0.0"
  },
  "devDependencies": {
    "prisma": "^6.19.2",
    "tsx": "^4.21.0"
  }
}